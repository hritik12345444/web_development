//5.  Print "you can drive" or "you cannot drive" based on age being greater than 18 using ternary operator

let age=17;
// console.log((age >= 18) ? "You can drive" : "you can't drive");  // ternary operator

let res= age >= 18 ? "You can dive" : "you can't drive"; // Ternargy operator
console.log(res);